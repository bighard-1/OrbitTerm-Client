#ifndef ORBITTERM_CBRIDGE_ORBIT_CORE_H
#define ORBITTERM_CBRIDGE_ORBIT_CORE_H

/* Rust owns the canonical ABI declaration. Keep this forwarding header free
 * of function declarations so Xcode cannot silently drift from Rust. */
#include "../../orbit-core/include/orbit_core.h"

#endif /* ORBITTERM_CBRIDGE_ORBIT_CORE_H */
