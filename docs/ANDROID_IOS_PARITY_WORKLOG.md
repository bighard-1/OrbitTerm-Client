# Android 与 iOS 行为对齐工作台账

## 目标与原则

Android 以 iOS 移动端的功能语义、信息架构和关键操作结果为基准；Android 的系统返回、输入法、无障碍、权限和窗口行为维持原生实现。任何“看起来相似”但会改变数据、连接安全边界或用户操作结果的实现都不视为完成。

发布级的逐项状态、负责人和验收条件见 [Android / iOS 行为对齐矩阵](ANDROID_IOS_BEHAVIOR_ALIGNMENT_MATRIX.md)；本文件仅保留过程台账与真机验收说明。

## 已确认的移动端信息架构

| iOS 入口 | Android 入口 | 行为状态 |
| --- | --- | --- |
| 服务器 | 服务器 | 已完成分组、搜索、批量选择、Host Key 信任、编辑与经过验证的连接。 |
| 会话 | 会话 | 已完成多会话工作区、终端、片段、监控、重连、清屏、复制粘贴与锁定清理。 |
| SFTP | SFTP | 已完成基于已验证会话的浏览、文本编辑、增删改权限、上传下载、ZIP、队列、进度与取消；待真实服务器真机验收。 |
| Docker | Docker | 已完成容器、统计、日志、启动停止重启暂停、强制停止与会话切换隔离。 |
| 个人中心 | 个人中心 | 已完成账户、安全、主密码、生物识别、同步、诊断和外观设置。 |

## 分阶段验收

1. **壳层与资产（已完成）**：五入口、服务器分组/搜索/批量操作、编辑与连接分离、Host Key 信任流程。  
2. **会话工作区（已完成）**：会话选择、关闭、重新连接、终端/快捷指令/监控/Snippets 四个模块及全屏输入体验。  
3. **SFTP（代码与模拟器验收完成）**：绑定当前已验证会话，目录浏览、传输、文件操作和批量操作；真实服务器真机验收待执行。  
4. **Docker（已完成）**：绑定当前已验证会话，容器列表、详情、日志与受控操作。  
5. **个人中心与设置（已完成）**：账户入口、应用与终端外观、安全和诊断。  
6. **跨端回归（待真实设备收尾）**：用同一份逐项用例在 Android 真机和 iOS 上验证关键结果；仅在差异有明确平台理由时保留差异说明。

## 项目收尾统一验收：真实服务器 SFTP 大文件

当前模拟器验证不替代真实网络与 Storage Access Framework 行为。该项已留档，待 Android 端功能开发完成后，由真机统一验收：

- 上传、下载大文件的实际字节进度：上传应显示已传输/总大小；无法可靠获得远端大小的下载只显示已传输量。
- 活动传输取消：取消后不出现半成品目标文件；上传临时远端文件与下载应用内暂存文件均须清理。
- FIFO 队列：顺序执行、等待项移除、失败暂停、显式继续及切换/断开会话后的安全终止。
- Android 10 与 Android 15 真机的 SAF 选取、目标文件写入、前后台切换和网络中断恢复提示。

## 本轮已完成的行为变更

- 两端生物识别生命周期统一为 fail-closed：前台进入锁定页每轮只自动验证一次；验证中禁用重复提交；系统注册变化、受保护密钥缺失或解密不一致时关闭旧开关，不再拿普通存储中的主密码静默重建，必须先以主密码解锁再由用户显式重新启用。
- 生物识别成功、失败、暂时锁定、不可用和失效采用同一固定词典；成功回执 4 秒后消失，失败/恢复提示持续显示；用户主动取消不制造错误提示。
- iOS 与 Android 的普通退后台路径均立即锁定并清理活动会话。Android 只对用户主动打开的系统文件选择器保留最多 2 分钟原生例外，旋转不误锁，超时或普通切后台不会继续持有可用主密码。
- 两端连接与同步主状态已收敛到固定词典；状态由类型化阶段驱动，故障原因保留为独立详情，Android 会话标题不再使用含义模糊的“终端在线”。
- Apple 同步完成态会核对 SSH 密钥库与端口映射配置结果；资产已同步但辅助配置失败时显示“同步失败”及脱敏详情，不再用“同步完成”掩盖部分失败。
- Android SSH 终端在原生层明确返回会话关闭或不存在后会保留工作区并切换为“已断开”，允许原生重连；暂时性写入错误不会误判为断线。
- 底部导航由“资产 / 会话 / 工具 / 设置”改为与 iOS 对应的“服务器 / 会话 / SFTP / Docker / 个人中心”。
- 服务器卡片的主要点按行为在两端统一为单击连接；移除 3 秒内二次点击确认，编辑保留独立入口。
- 会话页改为自身拥有工作区栏，支持选择现有会话、关闭当前会话和返回服务器。
- Android 底栏改为全宽 Material 3 `NavigationBar`，去除非标准悬浮外壳，布局层级与 iOS `TabView` 一致。
- Android 注册按钮直接反映邮箱、邀请码和 12 位组合密码规则；登录冷却显示剩余秒数并禁用提交。
- 终端附件“常用/符号”的内置集合与固定顺序已对齐；两端仍各自使用 UIKit 和 Compose 原生容器。
- 监控统一为六项摘要和六张趋势图，TCP 延迟与近 20 次失败率合并显示；iOS 补齐手动刷新、暂停/开始。
- 两端“关于”页均读取实际安装包版本，不再依赖写死文案。

## 当前未宣称完成的项目

- 真实服务器上的大文件上传、下载、取消、队列顺序和实际字节进度尚待真机验证。
- Android 10 与 Android 15 真机的 SAF、前后台切换、网络中断及跨账户竞态场景尚待最终回归。
- 发布签名、商店材料、灰度和 CI 发布门禁不属于当前本机开发验收范围。

## 2026-07-27：项目收尾保留项——真实传输验收

- 真机与真实服务器的 SFTP 大文件传输验收已正式延后至项目收尾统一执行；它不是当前功能开发的阻塞项，也不以模拟器结果替代。
- 验收范围固定为上传、下载、取消、FIFO 队列、进度、前后台切换与 Wi-Fi/蜂窝网络切换；同时覆盖 Android 10 与 Android 15。
- 使用 [真实设备与服务器验收记录](ANDROID_REAL_DEVICE_TEST_RECORD.md) 留存脱敏的环境、时间、最后进度、截图与诊断代码；失败记录不得被后续重试覆盖。

## 2026-07-26：Android 单跳跳板机（ProxyJump）

- Android 已原生接入 iOS 同一套单跳跳板语义：跳板与目标资产独立保存凭据、独立校验主机密钥；目标 SSH 会话建立在跳板的 `direct-tcpip` 通道内。
- 资产编辑器可启用跳板、配置地址/端口/用户名/私钥或密码认证、私钥口令、仅密钥策略和私钥失败后的可选密码回退；跳板私钥可从 SAF 文件选择器导入。
- 跳板元数据写入 Room v7，凭据只写 Android Keystore；加密同步载荷包含跳板配置及其独立凭据。禁用或更换跳板会清除不再引用的旧跳板凭据。
- 连接改走 checked SSH v2 JNI ABI。跳板主机密钥信任完成后会重新执行完整链路，随后再对目标主机发起独立信任；SFTP、Docker、终端和批量命令均复用最终验证后的会话。
- 已完成 core 跳板 FFI 测试、Android 单元测试、Debug 构建及 arm64-v8a/x86_64 JNI 符号检查；待真实跳板与目标服务器进行端到端验收。

## 2026-07-26：跳板机模块审查修复

- 修正跳板私钥认证的可选密码回退：只有策略允许时才把回退密码交给 core；策略关闭时不持久化该回退密码。
- 同步冲突影子改为比较跳板路由语义（地址、端口、用户名、认证策略），不比较带账户命名空间的本地凭据 ID，避免跨端同步产生伪冲突。
- 云端替换或移除跳板、以及本地禁用或替换跳板后，都会删除不再引用的 Keystore 跳板凭据。
- 本地保存先完成跳板配置与凭据前置校验，避免无效草稿留下孤立的目标凭据。

## 2026-07-27：服务器分组与应用主题对齐

- 服务器分组展开状态已从页面局部状态提升到资产 ViewModel：与 iOS 一样默认收起，并在切换会话、SFTP、Docker 或个人中心后保持用户选择。
- Android 保留“跟随系统 / 浅色 / 深色”外观模式，同时补齐 iOS 的五套应用配色：天空糖果、翡翠流光、蜜桃晨光、薰衣草雾、冰川薄荷。
- 应用配色独立持久化，不改变终端 ANSI 主题、字体大小或安全语义。

## 2026-07-26：批量命令安全语义复查

- 已对照 iOS `BatchCommandRunnerView`：所有目标均必须已有完成主机身份验证的 SSH 会话；不能自动连接，也不能静默跳过未连接目标。
- Android 已改为在执行前原子校验全部选中资产。任一资产缺少已验证会话时，整批命令不执行，并显示缺失会话数量与修复指引。
- 同一资产只会解析到一个活动会话，避免重复下发命令。
- Android 的批量选择模式已具备“选择此分组”入口，因此无需为该项重复新增一套分组选择界面。

## 2026-07-26：Docker 操作边界复查

- iOS 与 checked native core 均支持 Docker `kill`；Android 客户端此前未在参数白名单和界面中开放。
- Android 现仅在运行中容器的详情页提供“强制停止”，并要求二次确认；该操作不会挤占列表的日常启动、停止、重启和暂停入口。
- checked native bridge 仍会验证容器标识、操作枚举与已验证 SSH 会话，客户端没有引入 Shell 拼接或旁路网络访问。

## 2026-07-26：SFTP 在线文本编辑保存语义

- checked SFTP 写入会在发布替换文件前校验读取时的条目快照，远端文件已变更时返回 `sftp_entry_changed`，不会盲目覆盖。
- Android 编辑器改为只在保存成功后关闭；保存失败、冲突或临时目标占用时保留用户当前草稿，并在编辑器内说明原因。
- 保存进行中会暂时禁用再次保存和关闭操作，避免并发写入或请求尚未完成时误丢草稿。

## 2026-07-26：SFTP 打包下载队列一致性

- 目录或多项目“下载为 ZIP”已纳入与普通上传、下载相同的单任务 FIFO 队列；不会再绕过队列并发占用 SFTP 会话。
- ZIP 内的每个远端文件下载复用当前队列任务的取消标识，能够获得统一的原生传输进度与取消结果。
- 等待项、失败暂停、显式继续、会话切换时的安全终止及本地暂存清理均沿用现有传输状态机。

## 2026-07-26：SFTP 取消进度一致性

- 原生层拒绝取消（通常表示任务已接近完成）时，Android 只更新取消提示，不再使用点击取消时的旧状态回填界面。
- 因此，取消请求期间到达的最新字节进度不会被回退或覆盖。

## 2026-07-26：Docker 会话切换隔离

- Docker 容器列表、统计、操作回执和日志刷新均绑定发起请求时的已验证 SSH 会话标识。
- 会话切换后会立即取消旧日志自动刷新；迟到的旧会话响应不会覆盖新会话的容器或日志界面。
- 仅在实际底层会话变化时重置 Docker 页面，避免普通终端输出造成无谓刷新。

## 2026-07-26：账户切换中的同步隔离

- 同步启动时捕获账户作用域；资产和命令片段的后台读写均显式写入该作用域，不再依赖可能已变化的当前账户。
- 账户退出或切换期间完成的旧同步不会更新新账户的同步状态，也不会以旧凭据触发重跑或冲突回写。
- 最终真机验证需覆盖“同步进行中切换账户/退出登录”，确认旧账户数据不出现在新账户资产与命令片段列表中。

## 2026-07-26：异步会话与主密码回调隔离

- 令牌刷新、登录密码更新和主密码轮换在持久化令牌、更新 UI 或恢复内存主密码前，均确认原会话/原账户仍处于活动状态。
- 登出或账户切换后的迟到网络回调不会重新登录旧账户，也不会将旧账户主密码放回当前内存状态。
- 真机验收应覆盖：令牌接近过期时退出登录、登录密码修改中切换账户、主密码轮换中退出登录。

## 2026-07-26：锁定期间的终端打开竞态

- 终端异步打开请求现绑定发起时的会话代际；主密码锁定、登出或账户切换会使未完成请求失效。
- 若原生终端通道在安全边界之后才返回，Android 会立即关闭通道和关联 SSH 会话，而不会把它附着回已锁定界面。
- 关闭全部会话时也会清除未匹配的早期终端输出缓存。

## 2026-07-26：认证迟到回调隔离补充

- 登录请求拥有独立代际；登出后的旧登录成功或失败回调不再保存会话、激活旧账户或显示过期错误。
- 修改登录密码的失败回调同样先确认原会话仍有效，避免账户切换后向新页面写入旧账户的错误提示。

## 2026-07-28：P2 无障碍与终端输入基线

- 共享页面标题、空态标题、状态行与反馈横幅已补齐 Compose 语义：标题可被辅助服务定位，状态不只依赖颜色，成功/失败反馈通过礼貌实时区域播报。
- 终端原生视图提供不泄露输出内容的操作说明；远端输出、主机、路径与凭据不会作为辅助朗读文本暴露。预输入栏和模块切换补齐可读名称、Tab 语义与最小触控高度。
- 硬件键盘新增 Ctrl+A 至 Ctrl+Z、Enter、Backspace、Delete、Tab、Esc、Home、End、Page Up/Page Down 映射；现有 IME 提交、软键盘吸附与长按复制/粘贴逻辑保持不变。
- Android 15 仪器化测试已验证空态标题和动态反馈语义；JVM 测试已验证硬件键映射。TalkBack、字体 200%、横屏/分屏和真实硬件键盘仍按 `ANDROID_ACCEPTANCE_CHECKLIST.md` 的人工设备项验收，故矩阵状态保持“待完成”。

## 2026-07-28：P2 设计系统与视觉回归基线

- 应用层状态色已收束为 Neutral、Information、Success、Warning、Danger 五个语义令牌；状态色不参与终端 ANSI 色板计算。
- 危险确认与表单确认统一显示“危险操作”说明，使用风险色确认按钮并保留清晰的取消入口；既有调用方无需复制实现。
- 终端设置页改用共享 ANSI 色板预览；所有预置主题的终端前景与背景均由 JVM 门禁校验至少 4.5:1 对比度。
- Android 15 ARM64 模拟器新增两套无数据截图指纹基线：明亮状态/终端主题与深色危险确认。系统栏不参与截图，以避免设备时钟和导航区域产生误报。

## 2026-07-28：P2 真实交互验收准备

- Android 15 模拟器新增字体 200% 危险确认可达性、横屏终端状态与主题布局的仪器化回归；测试不读取账户、资产、终端或网络数据。
- 真机记录模板新增 A01–A07：字体 200%、TalkBack、窄屏、横屏、分屏、硬件键盘与软硬键盘切换。失败记录只保留脱敏的设备条件、可见行为和必要截图。
- TalkBack、实际分屏与真实硬件键盘仍需要最终设备交互证据，因此跨端矩阵无障碍项继续保持“待完成”。

## 2026-08-29：认证法律说明与运维模块状态契约

- iOS 与 Android 的登录/注册法律同意区统一为单行“已阅读并同意 / 查看法律条款”；完整“使用条款、免责声明与隐私说明”保留在辅助朗读与说明弹窗，窄屏不再依赖长链接自动换行。
- 监控、SFTP、Docker 增加同构的纯展示状态映射，固定加载、空数据、暂停、失败与就绪主标题；详细错误继续独立展示，不改变 checked 会话、传输、采样或容器操作逻辑。
- Android 补齐认证状态 Compose 回归、动态错误播报语义与三模块状态单元测试；Apple 补齐同词典映射测试。Android 固定画布视觉指纹已按当前稳定原生组件重新记录。

## 2026-08-30：运维模块操作与恢复契约

- iOS 与 Android 的监控、SFTP、Docker 刷新入口统一为“刷新 / 刷新中… / 重试”：忙碌期间显示原生进度并禁用重复提交，辅助功能名称明确当前动作和模块。
- 刷新或操作失败不会清空上一次成功的数据；界面在继续显示监控、目录或容器列表时，明确说明当前内容是可继续查看的旧数据，避免用户误认其仍为最新状态。
- 监控采样按钮统一为“暂停采样 / 恢复采样”，替代含义不完整的“停止 / 开始”；纯展示映射与静态跨端门禁覆盖所有统一文案，不改变 checked SSH、SFTP、Docker 或监控服务实现。

## 2026-08-30：运维结果反馈与确定性界面回归

- iOS 与 Android 的监控、SFTP、Docker 统一使用原生共享刷新、失败和成功反馈组件；成功结果展示 4 秒后自动消失，失败、部分失败及“仍显示旧数据”提示持续保留到用户重试或开始下一次操作。
- Android 的 SFTP 批量删除部分失败不再显示为成功，也不会被紧随其后的目录刷新覆盖；用户主动刷新时会明确结束上一条操作回执。
- Android Compose 回归覆盖三模块失败标题、旧数据说明、可访问重试、忙碌禁用及反馈寿命；iOS UI 测试通过无账户、无网络的固定启动场景验证同一套可见状态和四秒生命周期。

## 2026-08-30：同步、冲突与最近删除恢复状态

- Android 不再把含延后项目或待处理冲突的同步结果显示为“同步完成”；两端统一将其视为需要处理的同步失败，并继续保留已完成部分的脱敏摘要。
- 冲突标题和选择统一为“检测到同步冲突 / 保留本地修改 / 保留云端修改”，本地与云端摘要仍分区展示；两端均要求用户明确选择，不允许取消时静默采用云端版本，不改变冲突合并和加密同步算法。
- 最近删除统一加载、空态、失败、重试和旧记录保留文案；恢复或永久删除完成以及进入后台队列的回执显示 4 秒，失败持续保留。Apple 排队后的记录与 Android 一样先从当前列表移除，刷新时仍以服务端事实为准。
- Android Compose 与 iOS XCUITest 均使用无账户、无网络固定场景覆盖等待网络、等待解锁、部分失败、冲突操作、最近删除失败和回执寿命。

## 2026-08-30：账户安全操作状态与退出确认

- 登录密码、主密码轮换和生物识别提示不再共用 Android 的同一错误通道，避免某项失败显示到另一项表单；登录密码与主密码回执各自独立。
- 两端统一密码更新和主密码轮换的成功文案与 4 秒寿命；失败及需要完成本地提交的恢复状态持续保留，重试时才替换。
- Android 为密码变更与主密码轮换增加独立代际围栏；锁定、登出或账户作用域变化后，旧请求不能更新新会话。Apple 继续使用页面租约并为网络操作增加实际 30 秒超时竞争。
- iOS 和 Android 的“退出登录”均增加原生二次确认，明确会断开会话并清除登录状态，但不会删除按账户隔离的本机加密数据。
- 当前服务端未提供设备会话列表或按设备撤销接口，因此没有添加无后端语义的“设备解绑”入口；待 API、审计事件和令牌撤销契约齐备后另立阶段。

## 2026-08-30：剪贴板、敏感画面、通知与外部链接

- iOS 与 Android 建立同一剪贴板内容分级：凭据和私钥禁止复制；终端内容和主机指纹使用 60 秒条件清除与系统敏感/本机标记；公钥、容器 ID 与脱敏诊断保留普通复制。所有业务层直接写入剪贴板的路径已收口。
- Android 继续使用全局 `FLAG_SECURE`；iOS 在非活跃场景或录屏时覆盖解锁内容，并在系统截图通知到达时立即清理主密码等短期敏感输入。iOS 无法阻止系统截图是平台差异，不用伪造对等能力。
- Android 前台服务通知增加独立锁屏公开版本：解锁后仅显示有界会话数，锁屏不显示数量、账户、主机、用户、路径、命令、输出或凭据。
- `ssh://` 与 `orbitterm://connect` 在两端均改为仅打开审核页：拒绝嵌入密码、令牌、私钥、控制字符和超长字段；即使匹配现有资产也不再自动连接；锁定或审核页未就绪时保留待处理链接，且 Android Activity 不长期保留原始外部 URL。
- 本轮验收：Android 135 项 JVM 测试、26 项设备化测试与 16 项隔离冒烟场景通过，Lint、Debug/Release 构建通过；Apple 268 项 Checked 测试、10 项 iOS 功能/无障碍界面测试与 macOS/iOS Debug/Release 构建通过。iOS 性能采样用例在 Xcode 26.5 `DebuggerVersionStore`/CoreSymbolication 阶段卡住，与功能用例分离后未发现应用失败。

## 2026-08-30：网络切换、进程重建与会话恢复

- 两端将“会话可用网络”与“云同步公网已验证”分离：局域网 Wi-Fi/以太网中的 SSH 不会因缺少公网验证被误判不可用；无路由时重连按钮禁用并提供同义文本与无障碍提示。
- 网络恢复后只恢复用户手动重连入口，不自动发起 SSH/Telnet，也不绕过凭据、Host Key 或 Telnet 风险确认。
- 实时会话仍只由当前进程拥有。进程被系统终止后，两端只消费一个不含账户、资产、地址、命令或输出的布尔标记，明确提示旧原生会话未自动恢复。
- Apple 连接丢失路径会先撤销 verified lease、base/terminal handle 与监控面板的 UI 所有权，再串行关闭旧终端、SFTP、Docker、监控和 SSH；用户重连会等待该清理完成，避免旧资源或迟到回调覆盖新连接。
- Android 保留既有会话代际和操作作用域围栏，并新增网络/恢复策略单测与重连无障碍设备化回归；Apple 新增重连与进程标记单测。
- 本轮验收：Android 139 项 JVM 测试与 28 项设备化测试通过，Lint、Debug/Release 构建通过；Apple 270 项 Checked 测试通过，iOS 模拟器与 macOS Debug 构建通过。最终 ARM64 Debug APK 已覆盖安装至 Android 模拟器并完成无崩溃冷启动。

## 2026-09-01：冷启动、迁移与同步队列所有权

- Android WorkManager 同步授权改为“当前进程 + 本次主密码解锁”双重租约；租约只包含随机标识和不透明账户作用域，不含令牌、主密码或资产内容。旧版本布尔授权、旧进程任务以及同账户上一次解锁的任务均失效。
- Android 每次解锁使用独立的唯一工作名，同一解锁轮次继续去重；锁定或退出只取消该轮次任务。持久化资产 outbox 不被清除，重新解锁后由新租约继续处理，避免把安全取消误作数据丢弃。
- Android Room 继续禁止破坏性迁移；V1 至当前版本设备化测试除结构校验外，新增验证旧资产行完整保留在无归属分区，迁移不会擅自绑定后来登录的账户或制造同步任务。
- Apple 旧凭据迁移绑定账户作用域与操作代际；切换账户或退出后，旧迁移不能写回当前资产状态。任一 Keychain 写入失败均保留旧恢复源，不再误记迁移完成。
- Apple 旧 JSON 同步队列只有在所有项目成功提交或由 SQLite 去重后才删除原文件；事务失败执行回滚并保留恢复副本，避免冷启动升级期间静默丢失离线操作。
- 本轮验收：Android 141 项 JVM 测试、29 项设备化测试及隔离冒烟门禁通过，Lint、Debug/Release 构建通过；Apple 272 项 Checked 测试通过，iOS 模拟器与 macOS Debug 构建通过。最终 ARM64 Debug APK 已部署至 API 36 模拟器并完成无崩溃冷启动。

## 2026-09-01：本地存储异常恢复

- Android 冷启动在鉴权路由前执行 Room 完整性与 Keystore 会话可读性检查；数据库损坏、空间不足、数据库不可用、迁移中断和安全存储不可用均进入独立原生恢复页，不再伪装为退出登录或错误主密码。
- Android 登录、注册、令牌刷新、密码更新、主密钥轮换及退出登录的会话持久化失败统一 fail-closed：停止账户操作、保留最后可信会话和本地数据，只允许原地重新检查；Room 继续禁止破坏性重建。
- Apple Keychain 启动读取、令牌更新、主密码验证与退出登录不再静默吞掉访问失败；退出凭据未持久删除时保持账户锁定并显示恢复页，不宣称已经退出。
- Apple SQLite 同步队列新增打开结果、快速完整性检查与非破坏性重开；写入失败暂停发送并显示页顶恢复提示，重试不会删除、改名或重建原数据库，也不会绕过持久化队列直接提交。
- 本轮验收：Android 144 项 JVM 测试、30 项设备化测试、隔离冒烟、架构和跨端对齐门禁均通过，Debug、压缩 Smoke 与 Release 构建通过；Apple 274 项 Checked 测试、规范生成工程的 Checked 测试以及 iOS 模拟器/macOS 的 Debug 与 Release 构建通过，iPhoneOS ARM64 Release 源构建通过。iOS 模拟器 UI 运行门禁仍受本机 Xcode 26.5 `DebuggerVersionStore` 无法生成调试器版本影响，未将工具链卡死误报为应用通过；ARM64 Smoke APK 已覆盖安装并启动至 API 36 模拟器登录页。

## 2026-09-01：高压力同步积压切片

- 复核确认终端大输出、SFTP 传输队列与进度、Docker 长日志、监控长期采样均已有显式资源上限和确定性压力门禁，本轮不重复堆叠第二套状态机。
- Android 资产 outbox 不再一次性读入并遍历全部离线积压；每个 WorkManager 工作项最多读取 100 条 FIFO 记录，成功后以同一账户、同一进程和同一解锁租约追加下一工作项。正常积压不冒充网络失败，也不消耗指数退避次数。
- Android 只有在本地变更积压排空后才进入远端拉取、命令片段、SSH 密钥和端口映射同步，避免每个切片重复全量拉取；失败和冲突继续保留在 Room，不因页大小、进程重建或资源压力删除。
- Apple SQLite 队列继续逐条读取和单路投递；每成功投递 100 条后主动释放当前处理任务、让出执行权，再重新验证网络、账户令牌和队列所有权后续跑。持久化队列不设破坏性条目上限。
- 本轮验收：Android 145 项 JVM 测试、31 项设备化测试、隔离冒烟、架构、跨端对齐、Lint、R8、Smoke 与 Release 门禁通过；Apple 275 项 Checked 测试及 iOS 模拟器/macOS Release 构建通过。真机与真实服务器项目仍未执行。

## 2026-09-01：同步中断与安全续跑故障注入

- Android 分段续接与锁定/退出取消使用同一调度器互斥边界；取消先发生时旧租约不能追加工作，追加先发生时随后取消会撤销整条同租约工作链。旧工作不得在取消后把界面重新写成“同步中”。
- Android 的进程 epoch 保证系统杀进程或模拟器重启后，磁盘中遗留的旧 WorkManager 输入自动失效；只有用户在新进程中重新解锁取得新租约后，才能消费仍保留在 Room 的 outbox。
- Android 故障注入在专用测试数据库写入 237 条当前账户任务及另一账户任务，处理首批 100 条后关闭数据库并重新打开；剩余 137 条和另一账户记录均完整、FIFO 顺序不变且互不越权。
- Apple 完成一个 100 条切片后，除原处理租约外再次核验网络、非空令牌和预期账户；断网、锁定、退出或换账户任一发生均暂停旧续跑，由恢复后的当前账户重新触发。
- 本轮验收：Android 146 项 JVM 测试、32 项设备化测试与隔离冒烟门禁通过；Apple 276 项 Checked 测试通过。所有故障数据均为模拟器/进程内合成数据，未连接真实服务器，未执行真机项目。

## 2026-09-01：网络结果不确定与重复投递幂等性

- Apple 与 Android 的配置上传、移入最近删除、恢复及永久删除请求均携带 SHA-256 形式的稳定 `Idempotency-Key`；键只由密文请求或随机操作标识派生，不包含账户、主机、用户名、命令或明文凭据。
- Android Room 升级至 v9：每个待处理意图保存一次性生成的 `operationId`。v8 升级会为已有队列项原位补齐 UUID 形态的身份，重启、断网、超时及前台失败转后台均不重新生成操作；数据库迁移不删除原队列。
- Android 收到迟到成功响应时按 `operationId` 条件删除队列项，用户在请求期间产生的更新意图不会被旧响应误删。若上传已在服务端完成但响应丢失，下次拉取会以内存中的完整配置与凭据确认云端回显并提交同步基线，不产生第二次上传，也不持久化可供离线猜测的秘密摘要。
- 本轮故障注入覆盖：相同请求键稳定、不同请求键隔离、v8 待处理意图迁移和重开保持同一身份、旧响应不能删除新版意图，以及已接受上传的云端回显确认。真实服务器和真机仍不在本轮范围内。
- 本轮验收：Android 149 项 JVM 测试与 34 项模拟器设备化测试 0 失败，隔离冒烟、架构、跨端对齐、静态安全、Lint、R8 和 Release 门禁通过；Apple 278 项 Checked 测试 0 失败，iOS 模拟器与 macOS Release 构建通过。最新 ARM64 Smoke APK 已覆盖安装至 API 36 模拟器并完成强制停止后的冷启动。

## 2026-09-01：同步重放与冲突的安全可观测性

- Apple 与 Android 增加编译期白名单同步事件：未知网络结果已入队、后台投递延后、冲突延后、幂等回显确认，以及 Android 迟到响应被条件删除保护拒绝。诊断只导出事件类别和累计次数。
- 不保存事件关联时间线、账户、资产 ID、请求键、服务地址、异常正文、密文或明文凭据；用户切换账户或退出后计数立即清空，同一账户锁定期间保留本次进程内汇总，便于用户主动复制诊断。
- Apple 的网络请求明细仍使用既有 50 条有界、端点分类化记录；新增同步事件为独立聚合行。Android 在个人中心现有诊断文本中增加“同步事件”，不增加后台上传或第三方遥测。
- 本轮验收：Android 151 项 JVM 测试与 34 项 API 36 模拟器设备化测试 0 失败，隔离冒烟、架构、跨端对齐、静态安全、Lint、R8 与 Release 门禁通过；Apple 279 项 Checked 测试 0 失败，iOS 模拟器与 macOS Release 构建通过。最新 ARM64 Smoke APK（SHA-256：`f0276b0cd2b1616b83a637c83ec3d43c55eb6acbef186efab096ce890ebb8cfc`）已覆盖安装并通过强制停止后的冷启动。

## 2026-09-01：不可恢复同步错误的队列隔离与受控恢复

- Apple 与 Android 均按稳定错误代码划分投递行为：网络中断/超时进入系统退避，登录与解锁问题等待对应安全边界恢复，冲突等待用户选择，配置、协议、未知及超过有限重试次数的模糊服务拒绝进入持久化受阻状态。
- 受阻任务保留原始幂等身份和本机数据，但不再被每次后台同步选中。用户可在设置/个人中心明确选择“重新尝试受阻项目”；重试会重置有限尝试次数，不会生成新的业务操作身份。
- “丢弃受阻项目”必须经过原生危险操作确认，查询条件只允许删除当前账户的 `BLOCKED` 项；可自动重试、等待登录/解锁、冲突及其他账户任务均不受影响。本机资产和凭据不随队列意图删除。
- Android Room 由 v9 升级为 v10，原位增加投递状态、白名单失败代码及尝试次数；v9 既有任务迁移后均保持 `READY`，不凭空制造失败。迟到成功与迟到失败继续以 `operationId` 条件更新，不能覆盖新版用户意图。
- 两端诊断新增 `delivery_blocked` 聚合计数，仅记录类别与次数，不记录账户、资产、请求键、服务地址、异常正文或凭据。
- 本轮验收：Android 155 项 JVM 测试与 36 项 API 36 模拟器设备化测试 0 失败，架构、跨端对齐、静态安全、Lint、R8、Smoke 与 Release 门禁通过；Apple 282 项 Checked 测试与 11 项 iOS 模拟器 UI 测试 0 失败，macOS/iOS Debug、Release 及 iPhoneOS ARM64 源构建门禁通过。最新 ARM64 Smoke APK（SHA-256：`9f6fee24b71c942992ef117eb11dce55b7d300416f33d95ff13142842843a7f2`）已覆盖安装并通过强制停止后的冷启动；真实服务器与真机仍不在本轮范围内。

## 2026-09-01：同步 HTTP 状态契约与服务端退避

- Apple 与 Android 建立同一纯状态码恢复契约：401 等待重新认证；408、425、429 和 5xx 允许有界重试；其余 4xx 作为永久请求拒绝立即隔离；非 HTTP 成功状态按协议错误处理。错误正文不参与重试、冲突或恢复决策。
- 两端均解析 `Retry-After` 的秒数和标准 HTTP-date，拒绝无效、已过期与非正值，并把不可信超长值限制为 3600 秒；服务端建议只能延长、不能缩短客户端现有退避。
- Apple 对携带有效 `Retry-After` 的响应不在网络调用中长时间挂起，而是立即交给 SQLite 队列记录下一次投递时间；无建议值的可恢复响应仍采用可取消的短暂网络重试和队列指数退避。
- Android Room 升级至 v11，按账户和操作身份持久化 `nextAttemptAtUnix`。WorkManager 到期前不选择该任务；锁定、杀进程、重启或重新解锁均不能绕过服务端等待时间。普通网络失败继续使用系统退避，永久 4xx 不再进入无限后台循环。
- Android 登录凭据提交的 401 继续映射为认证失败，而已登录同步与令牌刷新的 401 映射为会话过期，避免统一 HTTP 策略破坏登录错误语义。
- 本轮验收：Android 159 项 JVM 测试与 38 项 API 36 模拟器设备化测试 0 失败，架构、跨端对齐、静态安全、Lint、R8、Smoke 与 Release 门禁通过；Apple 286 项 Checked 测试与 11 项 iOS 模拟器 UI 测试 0 失败，macOS/iOS Debug、Release 及 iPhoneOS ARM64 源构建门禁通过。最新 ARM64 Smoke APK（SHA-256：`eb16c914762b1337ad21d03fa76383803b960ea87bb783c209df6aa3eec679a3`）已覆盖安装并通过强制停止后的冷启动；真实服务器与真机仍不在本轮范围内。

## 2026-09-01：原生网络故障注入与延迟恢复闭环

- Android 在数据层增加仅供单元测试使用的 Ktor `MockEngine` 夹具，以真实 `HttpClient` 插件链返回 401、422、429 与 503；验证登录/注册的 401 上下文、永久 4xx 不受错误正文诱导、`Retry-After` 透传及 3600 秒上限，并断言每种服务端延迟响应只发出一次原生请求。
- Apple 为 `NetworkService` 增加仅在 Debug 编译条件可见的 `URLSessionConfiguration` 与重试时钟注入点；Release 不暴露备用传输。`URLProtocol` 脚本验证无服务端延迟的 503 最多尝试三次、429 携带延迟时立即交还持久队列、401 不重试，以及响应正文不能改变 422 的永久分类。
- Android 的 Room 设备化故障用例改为文件数据库：写入限流等待、等待解锁和另一账户任务后关闭并重新打开。重新解锁只释放等待解锁项，不会清除限流项的到期时间；另一账户队列独立可见，到期前后选择结果确定。
- Android 发布门禁现在显式运行 `core`、`domain`、`data`、`app` 和 `feature` 的全部 JVM 测试，并要求原生 HTTP 故障注入结果存在；数据层正式启用依赖锁定并提交测试引擎的完整解析图。设备化门禁同样要求延迟重开/解锁/账户隔离用例存在，避免新增契约只在开发者本机偶然执行。
- 本轮验收：Android 163 项 JVM 测试与 38 项 API 36 模拟器设备化测试 0 失败，架构、跨端对齐、R8、Lint、双 ABI Release 与隔离冒烟门禁通过；ARM64 Smoke APK（SHA-256：`dfd89da80b2cf9dbc008dd834951a23ab80c8156a9db9edace13ff9a9fc14506`）已覆盖安装并完成强制停止后的冷启动。Apple 290 项 Checked 测试 0 失败，macOS/iOS Debug、Release、iPhoneOS ARM64 源构建及两套 UI 测试目标编译通过。本机 Xcode 26.5.1 的 `DebuggerVersionStore` 在启动 iOS UI runner 前失败，重启 CoreSimulator 后仍可复现，因此本轮未把 UI 运行记为通过；未连接真实服务器或真机。

## 2026-09-01：传输中断、协议损坏与取消边界

- Apple 的原生请求执行器将可恢复范围从单一超时扩展为明确白名单中的短暂网络错误；超时、连接中断和截断响应最多进行三次可取消尝试。用户取消不在白名单中，立即向上传递，不会变成失败重试。
- Apple 对 200 但无法解析为约定信封的响应继续稳定归为协议异常；自动重试期间保持调用方提供的同一 `Idempotency-Key`。Debug 专用故障入口只复用正式请求构造器，不进入 Release 编译条件。
- Android 将超时与普通连接中断分别映射为稳定的网络超时、网络不可用错误；Ktor 成功响应的内容转换或结构解析失败改为 `remote_protocol_violation`，持久队列立即隔离该任务并保留本机数据，用户仍可通过既有受控恢复入口重试或丢弃。
- 两端测试均覆盖：超时、连接中断、截断/非法成功响应、取消不重试及幂等请求头稳定。发布门禁显式要求这些测试存在，避免仅保留测试类名却静默丢失关键用例。
- 本轮验收：Android 167 项 JVM 测试与 38 项 API 36 模拟器设备化测试 0 失败，架构、跨端对齐、静态安全、Lint、R8、双 ABI Release 与隔离冒烟门禁通过；ARM64 Smoke APK（SHA-256：`21a53557d94257813fba3561044ea806f29b82ab6e794e4d35eff62cc93eda5a`）已覆盖安装并完成强制停止后的冷启动。Apple 295 项 Checked 测试 0 失败，macOS/iOS Debug、Release、iPhoneOS ARM64 源构建及两套 UI 测试目标编译通过。本机 Xcode 26.5.1 仍因 `DebuggerVersionStore` 缺少调试器版本而无法启动 iOS UI runner，此工具链故障未记作应用测试通过；未连接真实服务器或真机。

## 2026-09-01：重试时钟、诊断收尾与账户租约原子化

- Apple 的网络退避状态改为严格配对的同步收尾：无论等待正常结束还是被取消/抛错，`retryInFlightCount` 都在请求返回前归零，不再依赖稍后执行的无所有权清理任务。
- Apple 与 Android 的持久截止时间继续负责跨进程和重启恢复；当前进程新增基于系统 uptime/elapsed realtime 的单调时钟守卫。普通时钟微调被接受，明显前跳或回拨只按真实经过时间推进，不能通过修改系统时间提前投递或无限延长等待。
- Apple 替换同步鉴权账户时立即失效旧处理租约、任务和定时唤醒，即使调用方遗漏先暂停也不能继续旧投递。Android 新账户解锁改为原子替换专用授权存储中的全部旧租约，相关方法串行化；旧 WorkManager 即使尚未被系统清除也会在下一边界检查安全退出。
- Apple 和 Android Release 门禁新增故障夹具泄漏检查：测试 URLProtocol、故障探针、Ktor MockEngine、测试类和测试域名均不得出现在产品二进制。时钟、账户切换和取消期间状态清理均成为强制回归。
- 本轮验收：Android 170 项 JVM 测试与 38 项 API 36 模拟器设备化测试 0 失败，架构、跨端对齐、静态安全、Lint、R8、双 ABI Release 与隔离冒烟门禁通过；ARM64 Smoke APK（SHA-256：`7e8b4bb8b47b7e6e37b0bdca8ee0c1c18fcac15bd0d4504336439e05cdce10cf`）已覆盖安装并完成强制停止后的冷启动。Apple 299 项 Checked 测试 0 失败，macOS/iOS Debug、Release、iPhoneOS ARM64 源构建及两套 UI 测试目标编译通过，Release 未发现故障夹具。本机 Xcode 26.5.1 的 `DebuggerVersionStore` 故障仍未解除，因此本轮未把 iOS UI runner 运行记为通过；未连接真实服务器或真机。

## 2026-09-02：公开发布界面与移动监控配置收尾

- Apple 登录页的后端切换能力拆为独立原生 ViewModifier：内部 Debug 构建继续提供隐藏触发、HTTPS 校验和自托管风险二次确认；`ORBITTERM_PUBLIC_RELEASE` 只编译无操作实现，不再把触发状态、地址输入和确认界面留在公开产品路径。
- iOS 移动端删除不能改变图表窗口的“趋势时间范围”假选项，与 Android 固定最近 5 分钟的六张趋势图一致；同一 SwiftUI 设置页在 macOS 仍保留实时/5/10 分钟选择，因为工作站监控详情真实消费该设置。
- Apple 发布门禁新增条件编译结构检查，并将后端调试界面文案加入 Release 排除扫描。移动/桌面条件分支分别完成构建，避免通过删除共享字段破坏 macOS 能力。
- 本轮沿用同一完整验证批次：Android 170 项 JVM 测试、38 项 API 36 模拟器设备化测试以及 Release/隔离冒烟门禁通过，最新 Smoke APK 已部署并冷启动；Apple 299 项 Checked 测试通过，iOS Debug/Release 与 macOS Release 在收尾修改后重新构建通过。本机 Xcode 26.5.1 的 iOS UI runner 工具链故障和真机验收仍单独保留，不作为代码通过证据。
