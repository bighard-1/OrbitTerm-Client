# OrbitTerm P2 平台真机验收记录

更新时间：2026-07-29  
目的：记录 iOS/iPad、macOS、Android 的原生交互与辅助功能证据。此清单不替代自动化门禁，也不记录账户、主机、路径、命令、密钥或终端输出。

## 通用准备

- 使用发布候选构建；记录版本、提交、设备型号、系统版本和测试日期。
- 使用专门的无敏感数据测试账户与测试资产；测试结束后删除资产和会话。
- 先运行对应自动化门禁；失败时不得以手工通过覆盖自动化失败。
- 记录“通过 / 失败 / 未执行”和可复现的非敏感步骤；不要截图或导出敏感终端内容。

## 自动化证据（不等同于真机验收）

| 日期 | 平台 | 结果 | 边界 |
| --- | --- | --- | --- |
| 2026-07-29 | Android 15 ARM64 AVD `OrbitTermReleaseSmoke_API35_ARM64` | unit 43 项、connected instrumentation 14 项，均 0 failures | 覆盖 Compose 语义、固定视觉基线、迁移与性能；不替代 TalkBack、真实硬件键盘、后台和 SFTP 真机验证。 |
| 2026-07-29 | macOS 本机 | 已构建并验证 Apple Development 签名的 `OrbitTerm.app` | XCUITest runner 在 `testmanagerd` 附着阶段无进展而停止；不是测试通过证据，需在受控签名 runner 重新执行。 |
| 2026-07-29 | iPhone 真机（iOS 26.4.2） | iOS UI 6 项，0 failures，`TEST SUCCEEDED` | 使用非持久测试根状态；覆盖未登录、锁定、已解锁空工作台、导航及登录/主密码控件可访问性。不登录、不同步、不连接真实服务器。 |

## iPhone / iPad

| 项目 | 验收步骤 | 通过条件 | 状态 / 证据 |
| --- | --- | --- | --- |
| VoiceOver 根状态 | 分别进入未登录、锁定、已解锁空工作台 | 标题、输入框、底部导航和恢复动作均可读且顺序合理 | 未执行 |
| Dynamic Type | 使用最大辅助字号浏览登录、资产、SFTP、Docker、个人中心 | 关键按钮、错误和危险确认不被截断或遮挡 | 未执行 |
| iPad 布局 | 竖屏、横屏、分屏下浏览资产、终端、SFTP、Docker | 无重叠；会话与返回路径可达；不承诺手机分屏终端 | 未执行 |
| 软硬键盘 | 终端输入、符号栏、外接键盘快捷键与收起键盘 | 输入焦点、快捷键与终端显示一致；无遗留键盘遮挡 | 未执行 |
| 后台/锁屏 | 已连接会话时切换应用、锁屏、返回应用 | 敏感内容受遮盖；恢复状态与底层连接一致；不承诺持续后台 SSH | 未执行 |
| 辅助显示 | Reduce Motion、Reduce Transparency、Increase Contrast、Differentiate Without Color | 状态不只靠颜色；面板、焦点和危险状态保持可读 | 未执行 |

## macOS

| 项目 | 验收步骤 | 通过条件 | 状态 / 证据 |
| --- | --- | --- |
| VoiceOver 与键盘 | 仅用键盘与 VoiceOver完成登录、锁定页、添加资产、会话切换、SFTP 与 Docker 操作入口 | 焦点顺序可预测；图标按钮有动作名称；危险操作不是默认焦点 | 未执行 |
| 窗口与会话 | 关闭窗口、关闭标签、切换账户、锁定与登出后观察会话 | 迟到回调不重开 UI；窗口关闭与会话关闭按产品文案一致 | 未执行 |
| 多窗口 | 打开多个工作窗口并切换焦点、关闭其中一个窗口 | 会话、账户和诊断不跨窗口混写；其他窗口不被意外关闭 | 未执行 |
| 显示与高对比 | 深浅色、Reduce Transparency、Increase Contrast 下检查工作台、SFTP、Docker、监控 | 终端 viewport 仍由 TerminalTheme 控制；应用主题不覆盖 ANSI palette | 未执行 |
| 签名 smoke | 受控签名候选构建上运行 `run_macos_signed_ui_smoke.sh` | XCUITest 实际启动、签名验证通过 | 未执行 |

## Android

| 项目 | 验收步骤 | 通过条件 | 状态 / 证据 |
| --- | --- | --- |
| TalkBack | 登录、主密码、资产、会话、SFTP、Docker、个人中心逐项浏览 | 标题、状态、错误、危险确认和恢复动作可理解；状态不只依赖颜色 | 未执行 |
| 大字号与横屏 | 200% 字号、横屏、分屏（如设备支持） | 危险确认、终端状态、导航和返回动作可达；无重叠 | 未执行 |
| 硬件键盘 | 终端输入、方向键、特殊键和会话切换 | 键映射符合终端说明；焦点不会误落入隐藏控件 | 未执行 |
| 后台与通知 | 前台服务期间锁屏、断网、恢复、账户切换 | 通知和实际连接一致；旧账户任务被取消 | 未执行 |
| SFTP | 大文件、取消、网络切换、SAF 选择与分享 | 队列、进度、取消和资源限制可预期，无宽泛存储权限 | 未执行 |
| Telnet | 同步导入一条 Telnet 资产并尝试连接 | 明确显示兼容但不支持；不创建 SSH 会话、不显示已验证、原始同步数据不被改写 | 未执行 |

## 发布结论

- 所有“未执行”项目完成并附上非敏感证据后，才能将对应平台体验标为真机验收完成。
- 任一安全、敏感遮盖、Host Key、账户隔离、同步写入或会话迟到回调问题均为发布阻断。
